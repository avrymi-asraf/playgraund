import json
import sys

print("hhloe form script")
for line in sys.stdin:
	print(line)
	print(1)
#    try:
 #       data = json.loads(line)
       # if "cells" in data:
      #      for cell in data["cells"]:
     #           if "outputs" in cell:
    #                cell["outputs"] = []
   #     print(json.dumps(data))
  #  except json.JSONDecodeError:
 #       print(line, end="")
#
